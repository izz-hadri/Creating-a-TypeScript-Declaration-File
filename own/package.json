{
  "name": "own",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "prepare": "npm run build",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/izz-hadri/Creating-a-TypeScript-Declaration-File.git"
  },
  "keywords": [
    "pluralsight",
    "greeting"
  ],
  "author": "Izz",
  "license": "ISC",
  "files": [
    "dist/**/*"
  ],
  "devDependencies": {
    "prettier": "^2.7.1",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.7.4"
  }
}
